<div class="container">
  <div class="row">
    {{#each posts}}
    <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
      <h3>{{this.title}}</h3>
      <p class="lead">{{this.body}}</p>
      <p>{{this.createdAt}}</p>
    </div>
    {{/each}}
  </div>
</div>